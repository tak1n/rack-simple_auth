cache: bundler
sudo: false

before_install:
  - gem install bundler

rvm:
  - 2.2.4
  - 2.3.0
  - ruby-head
  - jruby
  - rbx-3

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: rbx-3

script:
  - "COVERAGE=true bundle exec bin/spec_runner"

notifications:
  email:
    recipients:
      - benny.klotz92@gmail.com
    email:
      on_failure: change
      on_success: change
